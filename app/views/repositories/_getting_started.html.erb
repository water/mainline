<%
%>

<% help_box() do -%>
<h3>Getting started</h3>

<h4>Configure Git</h4>
<p class="hint">
  If you haven't done so already, download and install <a href="http://git.or.cz">Git</a>
</p>
<pre class="no-indent">
git config --global user.name "<%= current_user.fullname || "Your full name" -%>"
git config --global user.email "<%= current_user.email -%>"
</pre>

<h4>Pushing your local repository to Gitorious</h4>
<pre class="no-indent">
git checkout master
git remote add origin <%=h @repository.push_url %>
git push origin master
</pre>
<% end -%>
