<div class="comment">
  <div class="created_at"><%= comment.created_at.strftime("%B %d, %Y") %></div>
  <div class="content">
    <%= comment.body %>
  </div>
  <div class="actions">
    <%= link_to "Reply", new_comment_path(:parent_id => message) %> 
  </div>
</div>