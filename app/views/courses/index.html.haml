%table
  %thead
    %th Course code
    %th Examiner email
  %tbody
    - @courses.each do |course|
      %tr
        %td= course.course.course_codes.first.code
        %td= course.examiners.first.user.email
  