.hero-unit
  %h1 The courses you are registered for!
  
%table.table.table-bordered.table-striped
  %thead
    %th Course code
    %th Examiner email
  %tbody
    - @courses.each do |course|
      %tr
        %td= link_to course.course.course_codes.first.code,
          course_path("student", course)
        %td= course.examiners.first.user.email
  